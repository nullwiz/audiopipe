[pytest]
testpaths = test
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (deselect with '-k "not slow"')
    integration: marks tests that run the full pipeline

# Default timeout for tests (in seconds)
timeout = 300 